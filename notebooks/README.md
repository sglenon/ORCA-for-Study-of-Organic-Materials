# Notebooks

All notebooks run against bundled synthetic outputs. They do not require ORCA unless you deliberately launch a real calculation. Synthetic data are labeled in titles and should never be cited as chemical results.
